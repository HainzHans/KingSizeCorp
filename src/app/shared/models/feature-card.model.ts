/** Eine Leistungs-/Feature-Karte in der Angebots-Übersicht. */
export interface FeatureCard {
  /** PrimeIcons-Klasse ohne Präfix, z. B. 'pi-phone'. */
  icon:  string;
  title: string;
  text:  string;
}
